# Vue starter template

This tempalte uses bulma for UI components.

## Packages
Please check package.json for updated list
```
"accounting": "^0.4.1", // provides simple and advanced number, money and currency formatting
"bootstrap": "^4.0.0-beta.2", // Use either bootstrap or bulma
"bulma": "^0.6.1",
"croppie": "^2.5.1",    // image cropping plugin 
"flag-icon-css": "^2.8.0",
"gravatar-url": "^2.0.0", // Get the URL to a Gravatar image from an email
"idle-timeout": "^0.1.0",  // makes idle state detection in the browser
"jquery": "^3.2.0",
"lodash": "^4.17.4",    // utility library delivering modularity, performance & extras
"moment": "^2.19.1",    // Parse, validate, manipulate, and display dates and times
"moment-timezone": "^0.5.14", // Parse and display dates in any timezone
"popper.js": "^1.12.3", // EASILY POSITION TOOLTIPS, POPOVERS OR ANYTHING
WITH JUST A LINE OF CODE 
"tether": "^1.4.1", // efficiently making an absolutely positioned element stay next to another element 
"vue": "^2.5.3",
"vue-resource": "^1.3.4", // The plugin for Vue.js provides services for making web requests and handle responses
"vue-router": "^3.0.1", // official router of vue.js
"vue-server-renderer": "^2.5.3", //  offical offers Node.js server-side rendering for Vue 2.0
"vuex": "^3.0.1", // official Centralized State Management for Vue.js
"vuex-router-sync": "^5.0.0" // official Sync vue-router's current $route as part of vuex store's state.
```

## Build Setup

``` bash
# install dependencies
yarn install

# serve with hot reload at localhost:8080
yarn run dev

# build for production with minification
yarn run build
```
